#ifndef STRAT3_DATAMANAGER_ENUM_HEADER
#define STRAT3_DATAMANAGER_ENUM_HEADER

enum SaveFileOptions
{
    OneFile = 0,
    MultipleFile = 1
};
enum SaveOptions
{
    Erase = 0,
    Append = 1
};

enum FileType
{
    Binary = 0,
    CSV = 1
};

#endif
